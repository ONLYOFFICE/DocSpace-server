# Change Log

## 4.0.0

### Added

- Added the reworked AI API surface in `api/ai/`: `AIApi`, `AssignmentsApi`, `AttachmentsApi`, `EditorToolsApi`, `ExportApi`, `OpenAIPassthroughApi`, `PreferencesApi`, `ProfilesApi`, `PromptsApi`, `ThreadsApi`, `ToolsApi`, `WebSearchApi`
- Added per-user AI settings endpoints and the matching models `AiAiUserSettingsDto`, `AiAiUserSettingsWrapper`, `AiAiSettingsDto`, `AiAiSettingsWrapper`
- Added the `AppsApi` (`api/apps/`), `PrivacyRoomApi` (`api/rooms/`) and `DocsCloudApi` (`api/settings/`) APIs
- Added the `origin` configuration parameter, which sends the given value in the `Origin` request header

### Changed

- Renamed the API classes whose names collided in the package barrel export after the AI rework: the AI `SettingsApi` → `AISettingsApi` (`api/ai/aisettings-api.ts`), `FilesSettingsApi` → `SettingsApi` (`api/files/settings-api.ts`), `ProfilesApi` → `PeopleProfilesApi` (`api/people/people-profiles-api.ts`), `SettingsMessagesApi` → `MessagesApi` (`api/settings/messages-api.ts`)

### Removed

- Removed the superseded AI API classes `ChatApi`, `MCPApi`, `MessagesApi` and `ProvidersApi` from `api/ai/`
- Removed the superseded AI settings models `AiSettingsDto`, `AiSettingsWrapper`, `AiUserSettingsDto`, `AiUserSettingsWrapper` and `SetAiUserSettingsRequestDto`, replaced by `AiAiSettingsDto`, `AiAiSettingsWrapper`, `AiAiUserSettingsDto` and `AiAiUserSettingsWrapper`

## 3.7.0

### Added

- Added example.ts
- Added useSingleRequestParameter option
- Added new API methods and enhanced models with additional properties
- Added tag Rooms / Groups

### Changed

- Updated SDK OpenAPI specification v3.7.0
- Updated handling of array parameters with explode option
- Updated example values, added email length validation, and adjusted method return types in API models and methods

### Fixed

- Fixed & / ' issues

## 3.6.0

- Improved enum generation and data type consistency
- Updated API method descriptions and model fields
- SDK regenerated from OpenAPI specification v3.6.0

## 3.5.0

- Initial release